// piece.js
class Piece {
    constructor(type, ctx) {
      this.type = type;
      this.ctx = ctx;
      this.matrix = this.getMatrix();
    }
  
    getMatrix() {
      // code to get the matrix for the piece
    }
  
    drawPiece() {
      // code to draw the piece
    }
  
    rotatePiece() {
      // code to rotate the piece
    }
  }